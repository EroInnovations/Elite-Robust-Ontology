const AUTORUN=()=>{
    
  ENVIRONMENTCHECKER(ANDROIDDEVICE);
    
 };
 
 const ANDROIDDEVICE=()=>{
 
  ANDROIDHOMEPAGE();
 
 };
 
 const DESKTOPDEVICE=()=>{
 
  if (localStorage.getItem('UserData')) {
         
    } else {
         
  }
 
 };
 
 const WEBDEVICE=()=>{
 
  if (localStorage.getItem('UserData')) {
         
   } else {
         
  }
 
};